# demo-project — codebase context

<!-- AI context · source: demo-project · root: 058c0dd9 4 files · ~107 tokens · 39 tokens saved by dedup reproducible: same repo → same root hash. generated by bfx-ingest.  -->

## Files

```
  README.md
  helpers/util.js
  index.js
  lib/util.js  (= helpers/util.js)
```

### README.md

```markdown
# Demo Project

A tiny sample so you can see exactly what `bfx-ingest` produces.

Note: `lib/util.js` and `helpers/util.js` are identical on purpose, to show dedup in action.

```

### helpers/util.js

```javascript
// Shared helper.
function slugify(s) {
  return String(s).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');
}
module.exports = { slugify };

```

### index.js

```javascript
const { slugify } = require('./lib/util');

console.log(slugify('Hello, ButterflyFx World!'));

```
